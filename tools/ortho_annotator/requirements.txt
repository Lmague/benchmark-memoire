# Annotateur local d'orthomosaïques — dépendances runtime.
# Versions minimales alignées sur l'environnement vérifié du dépôt.
fastapi>=0.115
uvicorn>=0.30
rasterio>=1.4
fiona>=1.10
geopandas>=1.0
shapely>=2.0
pillow>=10.0
python-multipart>=0.0.9

# --- Prospection (facultatif) ---------------------------------------------
# scipy seul suffit à l'étage couleur ; torch + transformers ne servent qu'au
# détecteur dense et au re-classement. Sans eux l'outil démarre normalement et
# se rabat sur l'étage couleur.
scipy>=1.10
torch>=2.0
transformers>=4.40
