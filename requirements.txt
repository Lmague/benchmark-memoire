# Cœur du package (src/) + CLIs
torch>=2.4.0
torchvision>=0.19.0
timm
numpy
scikit-learn
pyyaml
pillow
matplotlib
tqdm

# Optionnels selon les modèles / l'usage
einops                # backbones ViT (DINOv3 / sslplant)
transformers>=4.56.0  # loader HF de dinov3_vitl16_lvd (AutoModel.from_pretrained)
huggingface_hub       # login pour le repo gated facebook/dinov3-*-lvd1689m
# seaborn, pandas     # uniquement pour les anciens scripts (legacy/)

# Backbones frozen externes (non pip-installables tels quels) :
#   DINOv3   : torch.hub.load("facebookresearch/dinov3", ...)  (poids + accès requis)
#   SimDINOv2: pip install git+https://github.com/ilyassmoummad/sslplant
#   SatMAE   : point d'extension (poids à sourcer — voir README §Extensions)
